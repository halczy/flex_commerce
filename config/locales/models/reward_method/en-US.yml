en-US:
  activerecord:
    models:
      reward_method: Reward Method
    attributes:
      reward_method:
        name: Name
        variety: variety
        settings: Settings
        variety_list:
          referral: Referral
          cash_back: Cash Back
    validators:
      reward_method:
        unique_variety: You can only add one of each type of reward method to product.