<td>
  <%= link_to 'Edit', edit_admin_shipping_method_path(shipping_method),
                      class: 'btn btn-sm btn-warning' %>
</td>
<td>
  <%= link_to 'Delete', '#', class: 'btn btn-sm btn-danger',
              id: "btn_del_#{shipping_method.id}",
              data: { toggle: "modal", target: "#delete_#{shipping_method.id}"} %>
</td>

<!-- Confirm Delete Modal -->
<div class="modal fade" id="delete_<%= shipping_method.id %>">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Delete Shipping Method</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <p>Please confirm that you want to delete this shipping method.</p>
        <ul>
          <li><%= shipping_method.name %></li>
        </ul>
      </div>
      <div class="modal-footer">
        <%= link_to 'Confirm', admin_shipping_method_path(shipping_method),
                               method: :delete,
                               id: "confirm_del_#{shipping_method.id}",
                               class: 'btn btn-danger' %>
        <button type="button" class="btn btn-secondary" data-dismiss="modal">
          Close
        </button>
      </div>
    </div>
  </div>
</div>
