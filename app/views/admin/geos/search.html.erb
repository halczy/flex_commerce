<%= provide :title, 'Geo Data Serach' %>

<div class="d-flex justify-content-start mb-3">
  <div class="p-2">
    <%= link_to admin_geos_path, class: 'btn btn-info' do %>
      <i class="fa fa-arrow-left" aria-hidden="true"></i> Return to List
    <% end %>
  </div>
  <div class="ml-auto p-2">
    <%= render 'search_form' %>
  </div>
</div>

<div class="row">
  <% if @search_result.present? %>
    <%= render 'shared/paginate', objects: @search_result %>
    <div class="col-lg-12">
      <strong>Result: </strong>
      <small><em><%= @search_run.count %> Matching Geo Data</em></small>
      <%= render 'geos', geos: @search_result %>
    </div>
    <%= render 'shared/paginate', objects: @search_result %>
  <% else %>
    <div class="col-12">
      <div class="jumbotron">
        <h5 class="text-center">No Matching Geo Data Found</h5>
      </div>
    </div>
  <% end %>
</div>
