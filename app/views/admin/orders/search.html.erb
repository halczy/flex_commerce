<%= provide :title, "Order Search" %>

<div class="d-flex justify-content-start mb-3">
  <div class="p-2">
    <%= link_to admin_orders_path, class: 'btn btn-info' do %>
      <i class="fa fa-arrow-left" aria-hidden="true"></i> Return to List
    <% end %>
  </div>
  <div class="ml-auto p-2">
    <%= render 'search_form' %>
  </div>
</div>

<div class="row">
  <% if @search_result.present? %>
    <%= render 'shared/paginate', objects: @search_result %>
    <div class="col-lg-12">
      <strong>Result: </strong>
      <small><em><%= @search_run.count %> Matching Orders</em></small>
      <%= render 'orders', orders: @search_result %>
    </div>
    <%= render 'shared/paginate', objects: @search_result %>
  <% else %>
    <div class="col-12">
      <div class="jumbotron">
        <h5 class="text-center">No Matching Product Found</h5>
      </div>
    </div>
  <% end %>
</div>
