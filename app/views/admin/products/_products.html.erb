<table class="table table-hover">
  <thead>
    <tr>
      <th>ID</th>
      <th class="w-25">Name</th>
      <th>SKU</th>
      <th>Member Price</th>
      <th>Reward Price</th>
      <th>Strict Inv.</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% products.each do |product| %>
    <tr>
      <td>
        <%= link_to product.id, admin_product_path(product) %>
      </td>
      <td>
        <%= link_to product.name, admin_product_path(product) %>
      </td>
      <td>
        <%= product.sku %>
      </td>
      <td>
        <%= humanized_money_with_symbol(product.price_market) %>
      </td>
      <td>
        <%= humanized_money_with_symbol(product.price_reward) %>
      </td>
      <td>
        <%= product.strict_inventory %>
      </td>
      <%= render 'action_control', product: product %>
    </tr>
    <% end %>
  </tbody>
</table>
