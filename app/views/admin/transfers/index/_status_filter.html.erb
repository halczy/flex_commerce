<%= form_with(url: admin_transfers_path, local: true, method: :get) do |form| %>
  <%= form.select :filter, options_for_select([
                  [t('.no_filter'), ''],
                  [t('.created'), 'created'],
                  [t('.pending'), 'pending'],
                  [t('.failure'), 'failure'],
                  [t('.success'), 'success']]),
                  { prompt: t('.filter_by_status') },
                  class: 'form-control', onchange: 'this.form.submit();' %>
<% end %>
