<%= provide :title, 'Inventories' %>

<div class="d-flex justify-content-start mb-3 mt-3">
  <div>
    <%= render 'filter' %>
  </div>
  <div class="ml-auto">
  </div>
</div>

<div class="row">
  <%= render 'shared/paginate', objects: @inventories if @inventories.present? %>
  <div class="col-lg-12">
    <p>Inventory Count: <%= @inventories.total_count %></p>
    <%= render 'inventories', inventories: @inventories %>
  </div>
  <%= render 'shared/paginate', objects: @inventories if @inventories.present? %>
</div>
