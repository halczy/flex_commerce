<%= form_with(url: admin_inventories_path, local: true, method: :get) do |form| %>
  <%= form.select :status, options_for_select([
                  [t('.no_filter'), ''],
                  [t('.unsold'), 'unsold'],
                  [t('.in_cart'), 'in_cart'],
                  [t('.in_order'), 'in_order'],
                  [t('.in_checkout'), 'in_checkout'],
                  [t('.sold'), 'sold'],
                  [t('.returned'), 'returned']]),
                  { prompt: t('.filter_inventories') },
                  class: 'form-control', onchange: 'this.form.submit();' %>
<% end %>
