<div class="card">
  <div class="card-header">
    <%= yield(:title) %>
  </div>
  <div class="card-body">
    <%= render 'wallets/shared/action_return' %>
    <div class="row">
      <%= render 'shared/paginate', objects: @transactions if @transactions.present? %>
      <div class="col-lg-12">
        <p>Transaction Count: <%= @transactions.total_count %></p>
      </div>
      <div class="col-lg-12">
        <%= render 'shared/no_record' if @transactions.empty? %>
        <% @transactions.each do |transaction| %>
          <%= render 'wallets/shared/transaction', transaction: transaction %>
        <% end %>
      </div>
      <%= render 'shared/paginate', objects: @transactions if @transactions.present? %>
    </div>
  </div>
</div>
