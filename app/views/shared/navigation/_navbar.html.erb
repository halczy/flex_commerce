<nav class="navbar navbar-expand-lg navbar-dark fixed-top bg-dark">
    <button class="navbar-toggler navbar-toggler-right" type="button"
            data-toggle="collapse" data-target="#navbarNavDropdown"
            aria-controls="navbarNavDropdown" aria-expanded="false"
            aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <%= link_to app_title, root_url, class: 'navbar-brand' %>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item <%= active?(root_url) %>">
          <%= link_to t('.home'), root_url, class: 'nav-link' %>
        </li>
        <%= render 'shared/navigation/menu' %>
      </ul>
      <ul class="nav navbar-nav">
        <%= render 'shared/navigation/shopping_cart' %>
        <% if current_user %>
          <%= render 'shared/navigation/account_dropdown' %>
        <% else %>
          <%= render 'shared/navigation/signin_signup' %>
        <% end %>
      </ul>
    </div>
</nav>
