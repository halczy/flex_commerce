<ul class="list-group">
  <%= link_to t('.dashboard'), dashboard_path(current_user),
              class: "list-group-item #{active_controller?('dashboards')}" %>
  <%= link_to t('.profile'), customer_path(current_user),
              class: "list-group-item #{active_controller?('customers')}" %>
  <%= link_to t('.wallet'), wallet_path(current_user),
              class: "list-group-item #{active_controller?('wallets')}" %>
  <%= link_to t('.order'), orders_path,
              class: "list-group-item #{active_controller?('orders')}" %>
  <%= link_to t('.reward'), reward_path(current_user),
              class: "list-group-item #{active_controller?('rewards')}" %>
  <%= link_to t('.address'), addresses_path,
              class: "list-group-item #{active_controller?('addresses')}" %>
</ul>
