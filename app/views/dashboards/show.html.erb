<%= provide :title, t('.title') %>

<%= render 'shared/header_breadcrumb', title: yield(:title) %>

<div class="container mt-5">
  <div class="row">
    <div class="col-3">
      <%= render 'shared/navigation/customer/sidenav' %>
    </div>
    <div class="col-9">
      <div class="jumbotron">
        <%= render 'dashboards/show/snapshot' %>
        <%= render 'dashboards/show/orders' %>
      </div>
    </div>
  </div>
</div>
